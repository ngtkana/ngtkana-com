# AIエージェント向け運用ルール

人間向けのドキュメントは [README.md](README.md) と `docs/` を参照。ここに書くのは、コーディングエージェントがこのリポジトリで作業する上での手順のみ。

## PRワークフロー

- 1つの論理的な変更 = 1PR。大きな変更（基盤の入れ替え、デザイン刷新など）は意味のある単位で複数PRに分割する
- PRを作成しCIが通ったら、そこで止める。**`gh pr merge` は実行しない**。マージは必ずユーザー自身が行う
- `main` へのpushで lint/test → デプロイの順に走る（デプロイはtest成功時のみ、[docs/DEPLOY.md](docs/DEPLOY.md)参照）

## 見た目・挙動の変更確認

Astroコンポーネントは自動テストで検証しにくい。見た目・挙動を変えたら以下を行う。

1. `npm run build && npm run preview`
2. puppeteer（devDependenciesに含まれる）でスクリーンショットを撮り、目視で確認する
   - デスクトップ・タブレット・モバイルの3幅
   - light / dark 両方の配色（`prefers-color-scheme` を切り替える）
   - キーボードのみでの操作（Tabでのフォーカス順序、フォーカスリングの視認性）

## ドキュメントの陳腐化を防ぐ

設計・規約・ディレクトリ構成に影響する変更をしたら、対応する `docs/*.md` とこのファイルを同じPRで更新する。ドキュメントの記述は「現在のファイル一覧」ではなく「責務・規約・なぜそうなっているか」を軸にし、ファイル増減で自動的に古びない書き方を保つ。
